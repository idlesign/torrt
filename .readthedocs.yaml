version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3"
  jobs:
    install:
      - pip install mkdocs-material mkdocs-navsorted-plugin mkdocs-apidescribed-plugin
      - pip install .

mkdocs:
  configuration: mkdocs.yml
